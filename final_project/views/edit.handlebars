<p>Welcome to the edit page!</p>


<form action="/" class="editForm" method="POST">
	<fieldset class="entryFieldset"><legend>Edit Workout: {{workout.name}}</legend>
	<p>Required fields are followed by <strong><abbr title="required">*</abbr></strong>.</p>
	<input type="hidden" name="id" value="{{workout.id}}">
	<p><label class="entryLabel" for="name"><span>Workout Name:<strong><abbr title="required">*</abbr></strong></span></label><input class="inputBox" type="text" name="name" value="{{workout.name}}"></p>
	<p><label class="entryLabel" for="reps"><span>Reps:</span></label><input class="inputBox" type="number" name="reps" value="{{workout.reps}}"></p>
	<p><label class="entryLabel" for="Weight"><span>Weight:</span></label><input class="inputBox" type="number" name="weight" value="{{workout.weight}}"></p>
	<p><label class="entryLabel" for="date"><span>Date:</span></label><input class="inputBox" type="date" name="date" value="{{workout.date}}"></p>

	<label class="entryLabel" for="unitsDiv">Units: </label>
	<div class="unitsDiv">
	<input type="radio" name="units" id="lbs" value="1" {{#if workout.units}}checked{{/if}}>
	<label for="lbs">lbs</label><br>
	<input type="radio" name="units" id="kg" value="0" {{#unless workout.units}}checked{{/unless}}>
	<label for="kg">kg</label>
	</div>
	<div class="button">
	<input type="submit" name="updateWorkout" value="Update Workout">
	</div>
	</fieldset>
</form>

<form class="editForm" action="/" method="POST">
	Canceling goes back to the main page without making any changes.<br>
	<input type="submit" value="Cancel Edit">
</form>